q - Can we change return type while performing overriding
Ans -- No , we cannot change return type while performing overriding
(When return type are primitive  e.g int , long , float , double)

Yes -- in case return type are ClassName (Covariant)

covariant means return type are class name and there is parent child relationship between those classes


runtime -- functionality get executed at runtime

late binding -- it binds after running the code

dynamic -- if method is not present in child class then it will search for it in parent class

WebDriver driver = new FirefoxDriver   -- this is implemented using polymorphism and inheritance


Q - Can we change access modifiers when overriding?
Ans- Yes , but we cannot reduce its visibility

Q - Can we override Static Methods

No

