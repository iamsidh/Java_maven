1. Create database studentmanager

2. create students table

query to create students table

create table students (id int auto_increment primary key,first_name varchar(20) not null,last_name varchar(20));